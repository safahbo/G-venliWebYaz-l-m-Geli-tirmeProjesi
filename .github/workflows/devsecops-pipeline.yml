name: Ultimate DevSecOps Pipeline

on: [push, pull_request]

jobs:
  sast-and-sca:
    name: Security Code Audit
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0 # Tüm geçmişi çek (TruffleHog için gerekli)

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with: { node-version: '18.x' }

    - name: 📦 SCA (Software Composition Analysis)
      run: |
        npm install
        npm audit --audit-level=critical

    - name: 🔑 Secret Scanning (TruffleHog)
      uses: trufflesecurity/trufflehog@main
      with:
        path: ./
        # Karşılaştırma hatasını engellemek için base/head kısımlarını kaldırdık
        # Sadece mevcut dizini ve commit'leri doğrulanmış şekilde tarar.
        extra_args: --debug --only-verified

    - name: 🔬 SAST (Static Application Security Testing) via Semgrep
      uses: returntocorp/semgrep-action@v1
      with:
        config: "p/owasp-top-ten"
